{% extends 'base.html.twig' %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/style/users.css') }}">
{% endblock %}

{% block title %}Utilisateur
{% endblock %}

{% block body %}
	<!-- Sidebar -->
	{{ include('_partials/_sidebar.html.twig') }}
	<!-- Navbar -->
	{{ include('_partials/_navbar.html.twig') }}

	<!-- Main content -->
	<div
		class="content">
		<!-- Card container -->
		<div class="card card-container small-card">
			<div class="card-body">

				<h1>Compte de
					{{user.firstname}}
					{{user.lastname}}
				</h1>


				{{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}

				<div class="mt-3">
				<a href="{{ path('app_user_index') }}">
					<i class="fa-solid fa-arrow-left mr-2"></i>Retour à la liste des utilisateurs</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
