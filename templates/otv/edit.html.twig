{% extends 'base.html.twig' %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/style/otv.css') }}">
{% endblock %}

{% block title %}OTV
{% endblock %}

{% block body %}
	<div class="showWrapper background">
		<div class="otvCard">
			<h1>OTV #
				{{otv.id}}</h1>

			{{ include('otv/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('app_otv_index') }}">back to list</a>

			{{ include('otv/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
