<form method="post" action="{{ path('app_o_t_v_delete', {'id': o_t_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ o_t_v.id) }}">
    <button class="btn">Delete</button>
</form>
