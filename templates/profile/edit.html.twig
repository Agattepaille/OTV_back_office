{% extends 'base.html.twig' %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/style/users.css') }}">
{% endblock %}

{% block title %}Mon compte
{% endblock %}

{% block body %}

	<!-- Sidebar -->
	{{ include('_partials/_sidebar.html.twig') }}
	<!-- Navbar -->
	{{ include('_partials/_navbar.html.twig') }}
	<!-- Main content -->
	<div
		class="content">
		<!-- Card container -->
		<div class="card card-container small-card">
			<div class="card-body">
				<h1>Mon compte</h1>
				{{ include('profile/_form.html.twig', {'button_label': 'Enregistrer'}) }}
				<div class="mt-3">
					<a href="{{ path('app_otv_index') }}">
						<i class="fa-solid fa-arrow-left mr-2"></i>Retours à la liste des OTV</a>
					{# {{ include('user/_delete_form.html.twig') }} #}
				</div>
			</div>
		</div>
	</div>

{% endblock %}
